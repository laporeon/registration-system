import path from 'node:path';

const filePath = path.resolve(__dirname, '../../src/data/formulario.txt');

const folderPath = path.resolve(__dirname, '../../src/data/users');

export { filePath, folderPath };
